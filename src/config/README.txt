# The config folder is used to keep the config files like :
#    a)Any configuration JS file which is used to route the user according to the role.
#    b)Routing files can be used through this folder.
    