# This folder is used to  create the models of the data which is recieved or which is sent using 
# the request or response and whatever data is required we create the models of the same object
# we initialize the object and we pass it to the components.
# Basically this files can work as an interface between the layers.